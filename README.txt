To compile code, have the following files in the same directory:

flow.txt
flow.txt.2
linked_list.c
linked_list.h
MFS.c
makefile

Then, run make in the directory where all the files are.
The program can now be run with ./mfs filename
